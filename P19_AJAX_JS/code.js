//AJAX, Esto es que sea de manera nativa, aquí no se usa por ejemplo Jquery o librerías o eso, la ventaja de este método es que no hay que utilizar una librería grande para realizar por ejmplo una única petición, de esta manera con el código nativo ya lo podemos simplificar

//Antes lo que se hacía era enviar peticiones y cuando el servidor nos devolvía una respuesta la página se recargaba completa, sin embargo con AJAX se envía la petición al servidor, y en paralelo el servidor se encarga se devolver una respuesta recargando únicamente la sección deseada

//PARA VER EJEMPLO SOBRE AJAX, ESTE PROY SE TRABAJARÁ EN OTRA CARPETA, REVISA LOS DOCS DEL XAMPP, AHÍ ESTÁ ALOJADO EL PROY ;)